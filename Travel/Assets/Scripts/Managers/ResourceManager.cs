using UnityEngine;
using System.Collections;
using Lucky;

public class ResourceManager : BaseInstance<ResourceManager> {

    /*
    public IEnumerator LoadAsset(out Object result)
    {
        result = null;
        ResourceRequest rr = Resources.LoadAsync(name);
        yield return rr;
        if (rr.asset != null)
            result = rr.asset;
        else
            result = null;
    }
    */
	
}
